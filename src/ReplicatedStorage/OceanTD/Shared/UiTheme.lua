-- Default UI theme for generated Ocean TD text / chrome.

local UiTheme = {
	-- Default for all generated text unless a system explicitly needs another face.
	Font = Enum.Font.FredokaOne,
}

return UiTheme
